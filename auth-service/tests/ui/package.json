{
  "name": "auth-ui-tests",
  "version": "1.0.0",
  "description": "Automated tests for the modern authentication UI",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "install-deps": "playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}